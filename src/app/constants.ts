// Application constants

// Time intervals (in milliseconds)
export const ROTATION_INTERVAL_MS = 2 * 60 * 1000; // 2 minutes - Tab auto-rotation in TV mode
export const REFRESH_INTERVAL_MS = 5 * 60 * 1000; // 5 minutes - Data auto-refresh in TV mode

// Treemap dimensions
export const MIN_TREEMAP_WIDTH = 1200;
export const MIN_TREEMAP_HEIGHT = 600;
export const DEFAULT_TREEMAP_WIDTH = 1920;
export const DEFAULT_TREEMAP_HEIGHT = 1080;
export const TREEMAP_PADDING = 32; // padding around treemap container

// Color scale boundaries (in percentage)
export const COLOR_CLAMP_MIN = -40;
export const COLOR_CLAMP_MAX = 40;

// Sparkline dimensions
export const SPARKLINE_WIDTH = 100;
export const SPARKLINE_HEIGHT = 30;
export const SPARKLINE_PADDING = 2;
