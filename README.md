
  # Branch Revenue Dashboard

  This is a code bundle for Branch Revenue Dashboard. The original project is available at https://www.figma.com/design/floYv9rB2SVtAv4PPL59ug/Branch-Revenue-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  